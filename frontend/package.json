{
  "name": "remember_all_frontend",
  "version": "1.0.0-SNAPSHOT",
  "description": "Project to remember all skills that i've ever had",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build:dev": "NODE_ENV=dev webpack --env=dev --progress --profile --colors",
    "build:prod": "NODE_ENV=production webpack --env=prod --progress --profile --colors",
    "build": "npm run build:prod",
    "start": "NODE_ENV=production webpack --host 0.0.0.0 --env=prod",
    "dev": "NODE_ENV=dev webpack-dev-server --content-base src --inline --hot --host 0.0.0.0 --port 8081 --env=dev --history-api-fallback"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "keywords": [
    "Dashboard"
  ],
  "author": "Oleksii Fisenko",
  "license": "Oleksii Fisenko",
  "homepage": "",
  "dependencies": {

  },
  "devDependencies": {

  }
}
